#include <iostream>
#include <string>
#include <cstdlib>
#include <ctime>

using namespace std;

int main()
{
    srand(time(0));
    int die1 = rand() % 6 + 1;
    int die2 = rand() % 6 + 1;
    
    if (die % 2 == 0) {
    cout << number << " is even. ";
    }
    if (die % 2 != 0) {
    cout << number << " is odd.";
    }



}